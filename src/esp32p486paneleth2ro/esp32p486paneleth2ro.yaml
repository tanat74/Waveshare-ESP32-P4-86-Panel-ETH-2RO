packages:
  substitutions: !include esp32p486paneleth2ro/common/substitutions.yaml
  audio: !include esp32p486paneleth2ro/services/audio.yaml
  loading: !include esp32p486paneleth2ro/common/pages/loading.yaml
  home: !include esp32p486paneleth2ro/pages/home/home.yaml
  light: !include esp32p486paneleth2ro/pages/light/config.yaml
  devices: !include esp32p486paneleth2ro/common/pages/devices.yaml
  alarm_panel: !include esp32p486paneleth2ro/pages/alarm_panel/config.yaml
  climate: !include esp32p486paneleth2ro/pages/climate/climate.yaml
  cover: !include esp32p486paneleth2ro/pages/cover/config.yaml
  fan: !include esp32p486paneleth2ro/pages/fan/config.yaml
  media_player: !include esp32p486paneleth2ro/pages/media_player/media_player.yaml
  radio: !include esp32p486paneleth2ro/pages/radio/radio.yaml
  vacuum: !include esp32p486paneleth2ro/pages/vacuum/vacuum.yaml
  voice_assistant_page: !include esp32p486paneleth2ro/pages/voice_assistant/voice_assistant.yaml
  button: !include esp32p486paneleth2ro/services/button.yaml

image: !include esp32p486paneleth2ro/common/images.yaml
font: !include esp32p486paneleth2ro/common/fonts.yaml
color: !include esp32p486paneleth2ro/common/colors.yaml

external_components:
  - source: github://alaltitov/esphome@dev
    components: [i18n]
    refresh: 1h

i18n:
  id: i18n_translations
  sources:
    - ${package_root}/translations/en.yaml
    - ${package_root}/translations/es.yaml
    - ${package_root}/translations/fr.yaml
    - ${package_root}/translations/nl.yaml
    - ${package_root}/translations/pl.yaml
    - ${package_root}/translations/ru.yaml
    - ${package_root}/translations/si.yaml
    - ${package_root}/translations/it.yaml
    - ${package_root}/translations/de.yaml
  default_locale: ru


esphome:
  name: ${name}
  friendly_name: ${friendly_name}
  name_add_mac_suffix: false
  platformio_options:
    lib_deps:
      - "bblanchon/ArduinoJson@^7.2.0"
  includes:
    - <sstream>
    - <algorithm>
    - <ArduinoJson.h>
  on_boot:
    - priority: 400
      then:
        - lambda: |-
            id(i18n_translations).set_current_locale(id(current_language));
        - script.execute: update_loading_page
    - priority: -100
      then:
        - lvgl.roller.update:
            id: backlight_settings_sleep_time_roller
            selected_index: !lambda return id(display_timeout);

esp32:
  board: esp32-p4-evboard
  variant: esp32p4
  flash_size: 32Mb
  cpu_frequency: 360MHz # было 400MHz
  framework:
    type: esp-idf
    components:
      - espressif/esp-sr==2.3.1
      - espressif/esp-dsp==1.7.0
    advanced:
      enable_idf_experimental_features: yes
    sdkconfig_options:
      CONFIG_LWIP_MAX_SOCKETS: "16"
      CONFIG_SPIRAM_FETCH_INSTRUCTIONS: y
      CONFIG_SPIRAM_RODATA: y
      CONFIG_CACHE_L2_CACHE_256KB: y
      CONFIG_ESP_TASK_WDT_TIMEOUT_S: "30"
      CONFIG_MBEDTLS_EXTERNAL_MEM_ALLOC: y
      CONFIG_ESP_HOSTED_SDIO_RX_Q_SIZE: "50"
      CONFIG_ESP_HOSTED_SDIO_TX_Q_SIZE: "50"
      CONFIG_SPIRAM_MALLOC_RESERVE_INTERNAL: "65536"

logger:
  level: debug
  logs:
    lvgl: info
  hardware_uart: UART0

# debug:

psram:
  mode: hex
  speed: 200MHz

esp_ldo:
  - channel: 3
    voltage: 2.5V

# ethernet:
#   type: IP101
#   mdc_pin: GPIO31
#   mdio_pin: GPIO52
#   clk: 
#     pin: GPIO50
#     mode: CLK_EXT_IN
#   phy_addr: 1
#   power_pin: GPIO51

esp32_hosted:
  active_high: true
  variant: ESP32C6
  reset_pin: GPIO54
  cmd_pin: GPIO19
  clk_pin: GPIO18
  d0_pin: GPIO14
  d1_pin: GPIO15
  d2_pin: GPIO16
  d3_pin: GPIO17

# update:
#   - platform: esp32_hosted
#     name: C6_update
#     path: network_adapter.bin
#     sha256: 1b1f6619689543c8a929db56db98a5663965bae9671a4a16436a90f4baa0f012

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  power_save_mode: none
  reboot_timeout: 10min
  fast_connect: false
  on_connect:
    - lvgl.label.update:
        id: wifi_status
        text_color: color_green
  on_disconnect:
    - lvgl.label.update:
        id: wifi_status
        text_color: color_steel_blue

network:
  enable_high_performance: true

display:
  - platform: mipi_dsi
    model: WAVESHARE-P4-86-PANEL
    reset_pin:
      number: 27
      inverted: false

i2c:
  sda: GPIO7
  scl: GPIO8
  scan: true
  frequency: 400kHz
  id: i2c_bus

touchscreen:
  platform: gt911
  id: touchscreen_
  # address: 0x14
  on_release:
      - if:
          condition: lvgl.is_paused
          then:
            - logger.log: "LVGL resuming"
            - lvgl.resume:
            - lvgl.page.show: home_page
            - lvgl.widget.redraw:
            - light.turn_on: display_backlight

output:
  - platform: ledc
    id: backlight_
    pin: GPIO26
    frequency: 100Hz
    inverted: true
    min_power: 0.0
    max_power: 0.8 # To prevent overheating due to excessive lighting
    
light:
  - platform: monochromatic
    name: "Подсветка"
    output: backlight_
    id: display_backlight
    restore_mode: ALWAYS_ON
    on_turn_on:
      - if:
          condition: lvgl.is_paused
          then:
            - logger.log: "LVGL resuming by backlight on"
            - lvgl.resume:
            - lvgl.page.show: home_page
            - lvgl.widget.redraw:
    on_turn_off:
      - if:
          condition:
            lambda: 'return id(display_timeout_backlight).state >= 0;'
          then:
            - logger.log: "Backlight off, pausing LVGL"
            - lvgl.pause:

api:
  encryption:
    key: !secret waveshare_key

ota:
  - platform: esphome
    id: ota_state
    password: !secret waveshare_ota
    on_begin: 
      - logger.log: "OTA Start"
      - light.turn_on: display_backlight
      - lambda: "id(display_backlight).loop();"
      - lvgl.resume:
      - lvgl.widget.redraw:
      - lvgl.label.update:
          id: ota_label_primary
          text: !lambda |-
            return id(i18n_translations).translate("ota.start.primary");
      - lvgl.label.update:
          id: ota_label_secondary
          text: !lambda |-
            return id(i18n_translations).translate("ota.start.secondary");
      - lvgl.widget.show: ota_widget
      - lvgl.resume:
      - lvgl.widget.redraw:
      - lambda: "id(lvgl_component).loop();"
      - lambda: "id(lvgl_component).loop();"
      - logger.log: "OTA widget shown"

    on_progress: 
      - lvgl.bar.update:
          id: ota_bar_value
          value: !lambda return (int)x;
      - lvgl.label.update:
          id: ota_label_primary
          text: " "
      - lvgl.label.update:
          id: ota_label_secondary
          text: !lambda |-
            static char buffer[64];
            snprintf(buffer, sizeof(buffer), id(i18n_translations).translate("ota.progress").c_str(), x);
            return buffer;
      - lambda: "id(lvgl_component).loop();"

    on_end:
      - logger.log: "OTA End"
      - lvgl.bar.update:
          id: ota_bar_value
          value: 100
      - lvgl.label.update:
          id: ota_label_primary
          text: !lambda |-
            return id(i18n_translations).translate("ota.end.primary");
      - lvgl.label.update:
          id: ota_label_secondary
          text: !lambda |-
            return id(i18n_translations).translate("ota.end.secondary");
      - lambda: |-
          id(lvgl_component).loop();

    on_error:
      - logger.log:
            format: "OTA update error %d"
            args: ["x"]
      - lvgl.label.update:
          id: ota_label_primary
          text: !lambda |-
            return id(i18n_translations).translate("ota.error.primary");
      - lvgl.label.update:
          id: ota_label_secondary
          text: !lambda |-
            return id(i18n_translations).translate("ota.error.secondary");
      - lambda: |-
          id(lvgl_component).loop();

lvgl:
  id: lvgl_component
  buffer_size: 100%
  byte_order: little_endian
  touchscreens:
    - touchscreen_id: touchscreen_
      long_press_time: 5000ms
      long_press_repeat_time: 400ms
  page_wrap: false
  top_layer:
    widgets:
      - obj:
          id: ota_widget
          hidden: true
          width: 720
          height: 720
          align: center
          clickable: false
          bg_color: color_slate_blue_gray
          border_opa: transp
          shadow_opa: transp
          radius: 0
          widgets:
            - label:
                id: ota_label_primary
                y: -75
                align: center
                text_font: nunito_20
                text_color: color_misty_blue
                text: "Starting OTA!"
            - label:
                id: ota_label_secondary
                y: -30
                align: center
                text_font: nunito_18
                text_color: color_misty_blue
                text: "Wait..."
            - bar:
                id: ota_bar_value
                y: 30
                width: 450
                value: 0
                align: center
                min_value: 0
                max_value: 100
                animated: true
                bg_color: color_steel_blue
                indicator:
                  bg_color: color_misty_blue


http_request:
  verify_ssl: false
